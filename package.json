{
  "name": "book-a-doctor",
  "version": "1.0.0",
  "description": "A full-stack doctor appointment booking application",
  "scripts": {
    "client": "cd client && npm start",
    "server": "cd server && npm run dev",
    "dev": "concurrently \"npm run server\" \"npm run client\""
  },
  "devDependencies": {
    "concurrently": "^8.0.0"
  }
}
